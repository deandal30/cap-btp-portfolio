_schema-version: "3.1"
ID: Portfolio
description: A simple CAP project.
version: 1.0.0
modules:
- name: Portfolio-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Portfolio-db
  - name: Portfolio-auth
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm-ci
- name: Portfolio-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: Portfolio-db
  parameters:
    buildpack: nodejs_buildpack
- name: Portfolio
  type: approuter.nodejs
  path: app/
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: Portfolio-auth
  - name: Portfolio-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: Portfolio-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Portfolio-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - febookshop.zip
      name: febookshop
      target-path: resources/
- name: febookshop
  type: html5
  path: app/fe-bookshop
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Portfolio-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: Portfolio-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: Portfolio-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: Portfolio-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Portfolio-html5-srv
    service-plan: app-host
- name: Portfolio-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Portfolio-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npx -p @sap/cds-dk cds build --production
